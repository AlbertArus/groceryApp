// import OCR from './OCR';

// function compararPrecios(textoOCR) {
//     // Lógica para extraer precios del texto del OCR (usando regex, etc.)
//     const preciosOCR = extraerPrecios(textoOCR);

//     // Lógica para obtener precios de la app (puede ser un array, objeto, etc.)
//     const preciosApp = obtenerPreciosApp();

//     // Lógica para comparar precios (devuelve un array con las diferencias, etc.)
//     const diferencias = compararPrecios(preciosOCR, preciosApp);

//     return diferencias; // Devuelve las diferencias encontradas
// }

// // ... (resto del código)

// <OCR image={capturedImage} compararPrecios={compararPrecios} /> // Pasa la función como prop